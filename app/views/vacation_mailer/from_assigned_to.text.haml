= l(:mail_body_vacation_from_author, :user => @user.name, :vacation_start => @vacation.start_date.strftime("%d.%m.%Y"), :vacation_end => @vacation.end_date.strftime("%d.%m.%Y") )

- @issues.each do |issue|
  = "* #{issue.project} - #{issue.tracker} ##{issue.id}: #{issue.subject}"

